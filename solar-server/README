To start:
> npm run start

To create a test database:
> node scripts/generate-test-db.js database.db

To install the package:
- Copy the provided `solar_meter.service` in `/lib/systemd/system/`
- Install the package under `/opt/solar_meter`
- Create the database
- Start it: `sudo service solar_meter start`
